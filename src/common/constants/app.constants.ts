export enum AppEnvironmentEnum {
  DEVELOPMENT = 'development',
  TEST = 'test',
  PRODUCTION = 'production',
  LOCAL = 'local',
}

export enum MorganFormatEnum {
  COMBINE = 'combined',
  COMMON = 'common',
  DEV = 'dev',
  SHORT = 'short',
  TINY = 'tiny',
}

export const CORRELATION_ID_HEADER = 'x-correlation-id';
